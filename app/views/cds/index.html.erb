
<div>
			<h2>CD一覧</h2>
			<table>
				<tbody>
					<th>imge</th>
					<th>pop</th>
					<th>在庫数</th>
					<th>金額</th>
					<th>アーティスト名</th>
					<th>タイトル</th>
				</tbody>
					<% @books.each do |book| %>
					<tr>

						<td><%= link_to user_path(book.user) do %>
						    <%= attachment_image_tag book.user, :photo, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
                             <% end %></td>

						<td><%= link_to book_path(book) do %>
						<%= book.title %>
						<% end %>
						</td>
						<td><%= book.body %></td>
						<td><%= book.body %></td>
						<td><%= book.body %></td>
						<td><%= book.body %></td>
						<td><%= link_to 'お気に入り登録' %></td>
						<td><%= link_to 'カートへ入れる' %></td>
						<td><%= link_to '購入ページへ' %></td>

                            <td><%= link_to '編集', edit_book_path(book) %></td>
　　　　　　　　　　　　　　　　 <td><%= link_to '削除',method: :delete, data: {confirm: 'Are you sure?'} %></td>


					</tr>
					<% end %>

			</table>

		</div>









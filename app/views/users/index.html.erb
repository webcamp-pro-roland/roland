<!-- 管・ユーザー一覧ページ -->
<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<!--

<% @users.each do |users| %>
 <%= attachment_image_tag @user, :image, class: "user_profile-img", fallback: "no_image.jpg",size: '100x100' %>
 <%= user.名前 %>
 <%= user.ニックネーム %>
 <%= link_to "詳細", user_path(user.id), method: :show, class: "show" %>
 <%= link_to "編集", user_path(user.id), method: :edit, class: "edit" %>
 <%= link_to "削除", user_path(user.id), method: :delete, class: "destroy" %>

<% end %>

-->